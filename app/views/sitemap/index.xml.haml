!!! XML
%urlset{:xmlns => "http://www.sitemaps.org/schemas/sitemap/0.9"}

  - for job in @jobs
    %url
      %loc #{job_url(job,:protocol => 'https')}/
      %lastmod= job.updated_at.strftime('%Y-%m-%d')
      %changefreq monthly
      %priority 0.6
